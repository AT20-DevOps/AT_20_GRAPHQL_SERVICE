pipeline {
    agent any
    environment {
            DOCKER_PASS = credentials('docker_pass')
    }
    stages {
        stage('Test') {
            agent { docker 'node:18-alpine3.16' }
            steps {
                sh 'npm install'
                sh 'npm test'
            }
            post {
                always {
                    archiveArtifacts artifacts: 'coverage/', followSymlinks: false
                }
            }
        }
        stage('Package'){
            steps{
                sh 'docker build -t at20_graphql .'
            }
        }
        stage('Publish'){
            steps{
                sh 'docker login -u pprfv -p ${DOCKER_PASS}'
                sh 'echo docker tag at20_graphql pprfv/pprfv:v1'
                sh 'echo docker push pprfv/pprfv:v1'
            }
        }
    }
}